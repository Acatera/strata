` %include "inc/std.inc"
section .text
    global _start
_start:
    InitStandardOutput()    
    `
    uint64 i = 10
    uint64 j = 20
    uint64 k = 0

    k = i * 10 + j / 5 ; `
    printf(`"eval = %d\n"`, [k])

    ExitProcess(0)`
