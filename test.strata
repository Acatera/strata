` %include "inc/std.inc"
section .text
    global _start
_start:
    InitStandardOutput()    
    `
    uint64 i = 10
    uint64 j = 20

eval = i + j / 5 ; `
    pop r15
    printf(`"eval = %d\n"`, r15)

    ExitProcess(0)`
